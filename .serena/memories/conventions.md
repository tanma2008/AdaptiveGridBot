# Conventions
- Versioned bot/tool filenames use explicit version suffixes such as `v048` and `v049`.
- Preserve existing local changes before Git synchronization; avoid destructive working-tree operations unless explicitly requested.
- Dashboard/reporting and trading-engine components are separate scripts; inspect targeted symbols/files before edits.