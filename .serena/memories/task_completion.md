# Task completion
- For source-sync tasks, verify `git status --short --branch` and `git log -1 --oneline` after synchronization.
- For coding changes, run the relevant project test/validation entrypoints when identified; do not invent a universal test command.