FILE POLICY: New active bot files use the naming pattern `adaptive_grid_bot_<BOT>_<SYMBOL>.py` (example: adaptive_grid_bot_A_btc.py). Existing/old files and legacy versions are valuable references and MUST NOT be deleted. Preserve old files; do not remove or overwrite them unless the user explicitly instructs it. Legacy/reference files are read-only unless the user explicitly says otherwise.