# AdaptiveGridBot core
- Source root: `D:\AdaptiveGridBot`; primary language Python.
- Git remote: `origin` → GitHub `tanma2008/AdaptiveGridBot`; branch `master`.
- Main areas include adaptive grid engine, OKX demo/reporting, ETH v0.48/v0.49 tooling, dashboard, risk/order/paper-trading modules.
- Project memories: `mem:tech_stack` for runtime/tooling; `mem:suggested_commands` for Windows command forms; `mem:conventions` for code conventions; `mem:task_completion` for completion checks.