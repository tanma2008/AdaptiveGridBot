NEXT TASK: Begin SOL work with G / SOL v4.8 only. Active filename must be `adaptive_grid_bot_G_sol.py`. Preserve all legacy SOL files; do not delete or modify reference files unless explicitly instructed. Follow workflow/current-operating-procedure: inspect existing SOL implementation and project state first, then build G v4.8, compile/diagnostics, dry-run, DEMO execution, re-run reconciliation/KEEP validation, then loop only after correctness is proven. Do not jump to H/I until G is stable.