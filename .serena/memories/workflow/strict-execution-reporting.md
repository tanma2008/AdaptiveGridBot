STRICT EXECUTION RULES: When the user gives a coding/editing command, Serena must actually execute the requested operation when the required tool is available; do not merely describe what should be done. After every edit/tool operation, explicitly report RESULT: SUCCESS or RESULT: FAILED. If FAILED, immediately report the exact tool/error message and stop that change rather than silently continuing or claiming completion. Never say a file was changed unless the edit tool returned success. Never say a test passed unless the test was actually executed and returned success. For multi-step work, report each completed step and the first failed step. Do not ask the user to repeat a command that can be executed with available tools. If a requested operation cannot be executed because the needed tool is unavailable, state that clearly. Keep active project/path explicit: current Work project is D:\AdaptiveGridBot; Server is C:\AdaptiveGridBot and must not be edited when Work is the active project. Preserve legacy/reference files unless explicitly instructed otherwise. Before switching bots, confirm the active bot file and current checkpoint.