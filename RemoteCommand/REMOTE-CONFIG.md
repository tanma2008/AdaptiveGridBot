# Remote Command Configuration

## Linux bot workspace

The canonical server-side AdaptiveGridBot workspace is:

`/home/amnat/AdaptiveGridBot`

All future PC → Linux copy/edit/run workflows for the bot must use this path unless explicitly changed by the user.

Do not use `/opt/adaptivegridbot` for the bot workspace.
